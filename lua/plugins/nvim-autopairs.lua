-- Auto pairs, which I really like. Should be better than mini pairs

return {
  'windwp/nvim-autopairs',
  event = 'InsertEnter',
  config = true,
  -- use opts = {} for passing setup options
  -- this is equivalent to setup({}) function
}
